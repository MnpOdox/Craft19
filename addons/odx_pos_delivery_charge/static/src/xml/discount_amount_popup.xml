<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="odx_pos_delivery_charge.DiscountAmountPopup">
        <Dialog title="props.title" bodyClass="'d-flex flex-column justify-content-center'" size="'sm'">
            <div class="input-symbol mb-3">
                <input
                    class="popup-input value active form-control form-control-lg text-center"
                    type="text"
                    inputmode="decimal"
                    t-att-value="props.formatDisplayedValue(this.state.buffer || '')"
                    t-att-placeholder="props.placeholder || ''"
                    t-on-input="onInput"
                />
            </div>
            <div t-if="props.showRefundToggle" class="d-flex justify-content-between align-items-center border rounded px-3 py-2 mb-3">
                <span class="fw-bold">Refund</span>
                <button
                    class="btn"
                    t-att-class="refundState.refundPositive ? 'btn-primary' : 'btn-outline-secondary'"
                    t-on-click="toggleRefundPositive"
                >
                    <t t-esc="refundState.refundPositive ? 'ON' : 'OFF'"/>
                </button>
            </div>
            <div t-if="props.feedback(this.state.buffer)" class="p-2 mx-auto mt-n2 mb-2" t-esc="props.feedback(this.state.buffer)" />
            <Numpad buttons="props.buttons"/>
            <t t-set-slot="footer">
                <div class="d-flex flex-grow-1 justify-content-center w-100">
                    <button t-att-disabled="!props.isValid(state.buffer)"
                        class="btn btn-primary btn-lg lh-lg o-default-button me-2 flex-fill"
                        tabindex="1"
                        t-on-click="confirm">
                        <t t-esc="confirmButtonLabel"/>
                    </button>
                    <button class="btn btn-secondary btn-lg lh-lg o-default-button flex-fill"
                        tabindex="1"
                        t-on-click="cancel">
                        Discard
                    </button>
                </div>
            </t>
        </Dialog>
    </t>
</templates>
